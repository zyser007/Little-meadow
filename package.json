{
  "name": "little-meadow",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cozy isometric farm — mobile-first web prototype (Godot port to follow)",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173 --strictPort",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
